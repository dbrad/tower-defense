Create instancable EntityManager
    - Entity Collections by components
    - Master entity Collections
    - Named entity retreival?
    - Sort entities by Tag via collections?
    

Create concept of attaching objects to scenes so nothing needs to live in the namespace or globally that belongs to a scene?

Global data should live in the Game.gameState object.

