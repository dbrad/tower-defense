Add call to render a rectangle to the screen.
 - look into ways of rendering without a texture?
 - just set the color directly to a color I think...
 - maybe needs a new call in the gl helper?
Use that call in pause blocker.
